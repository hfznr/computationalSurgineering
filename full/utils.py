import numpy as np
from typing import List


def separate_frames(frame_seq: np.ndarray) -> List[np.ndarray]:
    """
    Separates the frame sequence into individual frames stored in a list
    :param frame_seq: a frame sequence of 4 frames with the same width and height
    :return:
    """
    pass